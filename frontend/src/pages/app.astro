---
import Layout from '../layouts/Layout.astro';
import AppContainer from '../components/react/AppContainer';
---

<Layout title="Hermes - Sistema de Gestión de Pedidos">
  <div class="w-full h-screen flex flex-col bg-gradient-to-br from-blue-100 via-blue-50 via-white to-coral/20 overflow-hidden">
    <!-- Header -->
    <header class="bg-white/80 backdrop-blur-lg border-b border-blue-primary/10 py-4 px-4 md:px-8 relative z-50 shadow-sm">
      <div class="w-full max-w-7xl mx-auto flex justify-between items-center">
        <div class="flex items-center gap-3">
          <div class="w-10 h-10 bg-gradient-to-br from-blue-primary to-coral rounded-lg flex items-center justify-center">
            <span class="text-white font-bold text-lg">H</span>
          </div>
          <span class="text-xl font-bold text-blue-dark">Hermes</span>
        </div>
        <nav class="flex gap-6 items-center">
          <a href="/" class="text-blue-medium hover:text-blue-dark font-medium text-sm transition-colors hidden md:block">Inicio</a>
          <a href="/" class="px-4 py-2 bg-coral text-white rounded-full text-sm font-semibold transition-all duration-300 hover:bg-coral-dark hover:shadow-lg">
            Volver
          </a>
        </nav>
      </div>
    </header>
    <AppContainer client:only />
  </div>
</Layout>

